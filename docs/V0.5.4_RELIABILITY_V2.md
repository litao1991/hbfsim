# HBFSim v0.5.4 Reliability v2

每个 Block 维护 `read_count`，read RBER 由 base、P/E、read disturb 与 retention age
四项相加并受 retry scale 调整。`refresh.read_count_threshold` 非零时，达到阈值的
sealed Host-managed stripe 会复用 Refresh migration path。
