# HBFSim v0.5.5 Zone and Wear Leveling

`ZoneManager` 独立维护 logical zone 到 physical zone 的 Host-directed remap metadata，
并记录每 Zone 的 writes/erases、hottest/coldest 观察值。它不改变 Host-managed Stripe
的顺序 slot 与 generation 合约；重映射是上层 allocator 的 placement policy 输入。
